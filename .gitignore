node_modules
dist
dist-ssr
*.local
.DS_Store
coverage
*.tmp.ts

# Real PokerNow logs — contain other players' names and revealed hands.
# The repo is public; these must never be committed.
logs/
*.pokernow.csv
*.pokernow.json

# Graphify output. graph.json and .graphify_analysis.json are committed on
# purpose — they are the code map. The cache is regenerable build output, and
# .graphify_root records an absolute path from whichever machine ran it.
graphify-out/cache/
graphify-out/.graphify_root
